<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dbeginc.dbshopping">

    <application
        android:name=".DBShopping"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.dbeginc.dbshopping.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <!-- ############################## Splash  ############################## -->
        <activity
            android:name=".splash.view.SplashActivity"
            android:theme="@style/AppTheme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- ########################################################################################## -->
        <!-- ############################## Authentication ############################## -->
        <activity
            android:name=".authentication.login.view.LoginActivity"
            android:theme="@style/AppTheme.Login">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".authentication.signup.view.SignUpActivity"
            android:theme="@style/AppTheme.SignUp">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!-- ########################################################################################## -->
        <!-- ############################## Home Activity ############################## -->
        <activity
            android:name=".home.view.HomeActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!-- ########################################################################################## -->
        <!-- ############################## Add List Activity ############################## -->
        <activity android:name=".addlist.view.AddListActivity"
            android:theme="@style/AppTheme.AddList">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <!-- ########################################################################################## -->
        <!-- ############################## List Detail Activity ############################## -->
        <activity android:name=".listdetail.view.ListDetailActivity"
            android:theme="@style/AppTheme.ListDetail">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <!-- ########################################################################################## -->
        <!-- ############################## Item Detail Activity ############################## -->
        <activity android:name=".itemdetail.view.ItemDetailActivity"
            android:theme="@style/AppTheme.ItemDetail">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <!-- ########################################################################################## -->
    </application>

</manifest>